import { FunctionComponent } from "react";

// A simple wrapper for better intellisense
export type Component<T = {}> = FunctionComponent<T>;

// A simple wrapper for better intellisense
export type Page<T = {}> = FunctionComponent<T>;
